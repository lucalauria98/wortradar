# WortRadar - Abhaengigkeiten
# Installation:  pip install -r requirements.txt
# Danach einmalig (beste Lemma-Qualitaet):  python -m spacy download en_core_web_sm
streamlit>=1.40
spacy>=3.7
wordfreq>=3.0
lemminflect>=0.2.3
fsrs>=6.0,<7
pymupdf>=1.24
scipy>=1.11
numpy>=1.26
pandas>=2.0
requests>=2.31
supabase>=2.0          # Login/Auth (nur online noetig; lokal optional)
psycopg2-binary>=2.9   # direkter Postgres-Zugriff (Supabase) fuer die Daten
